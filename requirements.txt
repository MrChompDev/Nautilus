# Nautilus OS — Core Dependencies
# Python 3.11+ 64-bit / PySide6 Desktop Environment
#
# IMPORTANT: PySide6 requires 64-bit Python.
# If you have multiple Python versions, use the 64-bit one explicitly:
#   py -3.13 -m pip install -r requirements.txt
#   py -3.13 core/main.py

# Qt Framework (64-bit only)
PySide6>=6.5.0

# System monitoring (for Current telemetry app)
psutil>=5.9.0

# Required: Surfline password-vault encryption (AES-GCM)
cryptography>=42.0

# Optional: for production builds
# pyinstaller>=5.0

# Riptide audio hub (PySide6; requests + pygame)
requests>=2.31.0
pygame>=2.5.0
